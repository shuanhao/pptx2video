# Future Enhancements

以下項目目前先暫不實作，但後續若需要可優先加入。

## 已完成
- 解析 `.pptx` 的投影片編號、標題與 notes
- 支援多頁簡報、長段落、空白行與無 notes 頁面
- 輸出 JSON 結構化資料
- 提供 CLI 參數：`--output`、`--pretty`、`--verbose`、`--strict`
- 提供範例簡報生成與測試腳本

## 後續可加的功能

### 1. 批次處理多個 .pptx
- 支援一次處理多個簡報檔
- 可指定多個輸入檔或一個資料夾
- 輸出為多份 JSON 或一份彙整結果

### 2. 新增 `--skip-empty-notes`
- 若某頁沒有 notes，跳過該頁而不是視為錯誤
- 適合處理封面頁、結束頁、或沒有講稿的頁面

### 3. 新增 `--output-dir`
- 取代單一 `--output` 檔案輸出方式
- 可根據輸入檔自動建立輸出目錄與 JSON 檔名
- 適合批次處理與長期使用場景
